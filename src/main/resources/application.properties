kafka.broker.addr=${KAFKA_BROKER_ADDR:kafka:9092}
kafka.polling.delay.ms=${KAFKA_POLLING_DURATION_MS:5000}
kafka.polling.initial.delay.ms=${KAFKA_POLLING_INITIAL_DELAY_MS:1000}
kafka.odp.group.name=${KAFKA_GROUP_NAME}
kafka.error.consumer.mode=${IS_ERROR_QUEUE_CONSUMER:false}

management.endpoint.health.enabled=true
management.endpoint.health.show-details=ALWAYS
management.endpoints.enabled-by-default=true
management.endpoints.web.base-path=/officer-delta-processor
management.endpoints.web.path-mapping.health=/healthCheck
server.port=8080
chs.api.key=${CHS_INTERNAL_API_KEY}
api.url=${API_URL}
payments.api.url=${PAYMENTS_API_URL}
internal.api.url=${INTERNAL_API_URL}

logging.level.root=INFO
